algoritmo "raiz quadrada"

var

num, n, raiz: inteiro

inicio

Escreva("Digite um número: ")
leia(num)

n <- 0
raiz <- 0
Repita

    n <- n + 1
      Se (n %2 <> 0) entao
        num <- num - n
        raiz <- raiz + 1
      FimSe





Ate (n >= num)

Escreva(raiz)

fimalgoritmo